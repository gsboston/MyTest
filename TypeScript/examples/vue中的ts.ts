// 编译型语言，预编译，作用，因为有编译器就可以在语言层面做很多js做不到的事情。
// ts下写js，静态类型注解、检查，作为强类型语言，所有数据有类型，约束。对代码的控制增强，适用于大项目
// 减少随意发挥部分。有问题在编译阶段就发现，不要等到运行时才报错。
// 类的实现，面向对象编程，es6也有增强，不过还是有一定差距，如接口。

// ng=>ts vue=>ng  react=>函数式编程

